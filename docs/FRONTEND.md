# Frontend Overview

## Scope
- 프론트엔드는 React + Vite 기반이다.
- 주요 역할은 사용자 메시지 입력, 응답 렌더링, thread continuity 유지다.

## Backend contract
- `/api/v1/agent/invoke`
- `/health/live`
- `/health/ready`

## Notes
- 프론트엔드는 백엔드 계약을 바꾸지 않는 범위에서 UI를 발전시켜야 한다.
